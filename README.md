# Mac Status Bar App
An example of an NSStatusBar (mac menu bar) item in Rust using the `objc2` and `objc2_app_kit` crates, including buttons that call rust functions and a title that refreshes on open.
